[project]
name = "iris-gui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "elevate>=0.1.3",
    "pyduinocli",
    "requests>=2.32.4",
]

[tool.uv.sources]
pyduinocli = { path = "../pyduinocli", editable = true }
